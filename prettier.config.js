const config = {
  singleQuote: true,
  overrides: [
    {
      files: '*.html',
      options: {
        singleAttributePerLine: false,
        printWidth: 200,
      },
    },
  ],
};

export default config;
